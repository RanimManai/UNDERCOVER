import { useState } from "react";
function Success(){
    return <div className="Username" style={{ display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}>
        <h2>Success</h2>
        <div className="buttons">
            <button > Ready</button>
        </div>
    </div>
}
export default Success;
